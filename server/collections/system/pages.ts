import { defineCollection } from ".";

export const pages = defineCollection({
  display: {
    plural: "Pages",
    singular: "Page",
    icon: "lucide:zap",
  },

  name: "pages",

  fields: {
    //
  },
});
